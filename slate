# Configs
config defaultToCurrentScreen true
config windowHintsShowIcons true
config windowHintsIgnoreHiddenWindows false
config windowHintsSpread true

# Monitor aliases
alias mon-air 1440x900
alias mon-dell 1680x1050

# Default layouts
default 1monitor resolutions:${mon-air}

# Position aliases
alias full move screenOriginX;screenOriginY screenSizeX;screenSizeY
alias half-left move screenOriginX;screenOriginY screenSizeX/2;screenSizeY
alias half-right move screenSizeX/2;screenOriginY screenSizeX/2;screenSizeY
alias 2-throw-left throw 0 resize
alias 2-throw-right throw 1 resize

# 1 monitor layout
#layout  1monitor 'Spotify' ${1-main}

# Move/Resize bindings
bind f:cmd;shift ${full}
bind left:cmd;shift ${2-throw-left}
bind right:cmd;shift ${2-throw-right}
bind right:alt;cmd ${half-right}
bind left:alt;cmd ${half-left}

# Push bindings
bind right:ctrl;shift push right bar-resize:screenSizeX/2
bind left:ctrl;shift push left center

# Nudge bindings
bind right:ctrl;alt nudge +10% +0

# Focus bindings
bind left:cmd focus left
bind right:cmd focus right

# Hint bindings
bind esc:cmd hint

# Layout bindings
#bind l:cmd layout 1monitor

# Grid bindings
bind esc:ctrl grid
